<odoo>
    <data>
        <record id="ks_realtor_sign_view_picking_form" model="ir.ui.view">
            <field name="name">ks.realtor.sign.stock.picking.tree</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form" />
            <field name="arch" type="xml">

                <xpath expr="//field[@name='move_ids_without_package']//tree//field[@name='product_packaging_id']" 
                    position="replace">
                        <field name="product_packaging_id" groups="product.group_stock_packaging"
                            context="{'default_product_id': product_id}"
                            readonly="not product_id"
                            optional="'hide' if parent.is_picking_type_realtor_sign_installation == True else 'show'" />
                </xpath>

                <field name="company_id" position="after">
                    <field name="is_picking_type_realtor_sign_installation" invisible="1"/>
                </field>

                <field name="picking_type_id" position="attributes">
                    <attribute name="readonly">is_picking_type_realtor_sign_installation == True</attribute>
                </field>

            </field>
        </record>
    </data>
</odoo>